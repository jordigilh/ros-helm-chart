# E2E Validator Requirements
# Standalone - No IQE or external framework dependencies
# NOTE: Database queries use kubectl exec - no psycopg2 needed!

kubernetes>=28.1.0
boto3>=1.34.0
celery>=5.3.4
redis>=5.0.1  # Standard Python client (works with Valkey)
requests>=2.31.0
click>=8.1.7
urllib3>=2.1.0
PyYAML>=6.0.1  # For nise YAML parsing
koku-nise>=2.0.0  # Test data generator

# Optional but recommended
colorama>=0.4.6  # Cross-platform colored output
tabulate>=0.9.0  # Pretty tables


{{- if and .Values.ui (eq (include "cost-onprem.platform.isOpenShift" .) "true") }}
{{- $cookieSecret := (lookup "v1" "Secret" .Release.Namespace (printf "%s-ui-cookie-secret" (include "cost-onprem.fullname" .))) -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "cost-onprem.fullname" . }}-ui-cookie-secret
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cost-onprem.labels" . | nindent 4 }}
    app.kubernetes.io/component: ui
type: Opaque
data:
  session-secret: {{ if $cookieSecret }}{{ index $cookieSecret.data "session-secret" }}{{ else }}{{ randAlphaNum 32 | b64enc }}{{ end }}
---
{{- $clientSecret := (lookup "v1" "Secret" .Release.Namespace (printf "%s-ui-oauth-client" (include "cost-onprem.fullname" .))) -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "cost-onprem.fullname" . }}-ui-oauth-client
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cost-onprem.labels" . | nindent 4 }}
    app.kubernetes.io/component: ui
type: Opaque
data:
  {{- if $clientSecret }}
  client-id: {{ index $clientSecret.data "client-id" }}
  client-secret: {{ index $clientSecret.data "client-secret" }}
  {{- else if and .Values.ui.oauthProxy.client.id .Values.ui.oauthProxy.client.secret }}
  client-id: {{ .Values.ui.oauthProxy.client.id | b64enc }}
  client-secret: {{ .Values.ui.oauthProxy.client.secret | b64enc }}
  {{- else }}
  {{- $secretName := printf "%s-ui-oauth-client-secret" (include "cost-onprem.fullname" .) -}}
  {{- fail (printf "OAuth client credentials must be provided via .Values.ui.oauthProxy.client.id and .Values.ui.oauthProxy.client.secret or create secret '%s' with keys 'client-id' and 'client-secret' manually" $secretName) }}
  {{- end }}
{{- end }}

{{- if eq (include "cost-onprem.platform.isOpenShift" .) "false" }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "cost-onprem.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cost-onprem.labels" . | nindent 4 }}
  annotations:
    {{- with .Values.serviceIngress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- if .Values.serviceIngress.className }}
  ingressClassName: {{ .Values.serviceIngress.className }}
  {{- end }}
  {{- if .Values.serviceIngress.tls }}
  tls:
    {{- range .Values.serviceIngress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.serviceIngress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
        # Main API fallback (paths not handled by dedicated Ingresses)
        - path: /
          pathType: Prefix
          backend:
            service:
              name: {{ include "cost-onprem.fullname" $ }}-ros-api
              port:
                number: {{ $.Values.ros.api.port }}
        # File upload service (ingress)
        - path: /api/ingress
          pathType: Prefix
          backend:
            service:
              name: {{ include "cost-onprem.fullname" $ }}-ingress
              port:
                number: {{ $.Values.ingress.server.port }}
        # Ready endpoint for ingress service health checks
        - path: /ready
          pathType: Exact
          backend:
            service:
              name: {{ include "cost-onprem.fullname" $ }}-ingress
              port:
                number: {{ $.Values.ingress.server.port }}
        # ROS API (main API endpoints) - with Host header fix via server-snippet
        - path: /api/ros
          pathType: Prefix
          backend:
            service:
              name: {{ include "cost-onprem.fullname" $ }}-ros-api
              port:
                number: {{ $.Values.ros.api.port }}
        # Sources API (source management)
        - path: /api/sources
          pathType: Prefix
          backend:
            service:
              name: {{ include "cost-onprem.fullname" $ }}-sources-api
              port:
                number: {{ $.Values.sourcesApi.port }}
        # MinIO Console (object storage UI) - only for Kubernetes
        {{- if (eq (include "cost-onprem.platform.isOpenShift" $) "false") }}
        - path: /minio
          pathType: Prefix
          backend:
            service:
              name: {{ include "cost-onprem.fullname" $ }}-minio
              port:
                number: {{ $.Values.minio.ports.console }}
        {{- end }}
    {{- end }}
{{- end }}

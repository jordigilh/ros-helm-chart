{{- if .Values.kafka.bootstrapServers }}
apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cost-onprem.labels" . | nindent 4 }}
    app.kubernetes.io/component: kafka-alias
spec:
  type: ExternalName
  externalName: {{ include "cost-onprem.kafka.host" . }}
  ports:
    - port: {{ include "cost-onprem.kafka.port" . }}
      targetPort: {{ include "cost-onprem.kafka.port" . }}
      protocol: TCP
      name: kafka
{{- end }}
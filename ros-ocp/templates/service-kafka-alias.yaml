{{- if .Values.kafka.bootstrapServers }}
apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ros-ocp.labels" . | nindent 4 }}
    app.kubernetes.io/component: kafka-alias
spec:
  type: ExternalName
  externalName: {{ include "ros-ocp.kafkaHost" . }}
  ports:
    - port: {{ include "ros-ocp.kafkaPort" . }}
      targetPort: {{ include "ros-ocp.kafkaPort" . }}
      protocol: TCP
      name: kafka
{{- end }}
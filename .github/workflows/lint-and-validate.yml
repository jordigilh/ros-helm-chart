name: Lint and Validate Helm Chart

on:
  pull_request:
    branches: [ main, master ]
    paths:
      - 'cost-management-onprem/**'
  workflow_dispatch:

jobs:
  helm-validation:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Install Helm
      run: |
        curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash
        helm version

    - name: Lint Helm chart
      run: |
        echo "=== Linting Helm chart ==="
        cd cost-management-onprem
        helm lint .

    - name: Validate Helm templates
      run: |
        echo "=== Validating Helm templates ==="
        cd cost-management-onprem

        # Template validation without cluster connection
        echo "Running template validation..."
        helm template cost-mgmt-test . --debug --dry-run
        echo "✓ Helm template validation completed successfully"




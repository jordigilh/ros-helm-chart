{{- if .Values.infrastructure.redis.persistence.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-data
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cost-mgmt.labels" . | nindent 4 }}
    app.kubernetes.io/component: cache
    app.kubernetes.io/name: redis
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.infrastructure.redis.persistence.storageClassName }}
  storageClassName: {{ .Values.infrastructure.redis.persistence.storageClassName }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.infrastructure.redis.persistence.size | default "5Gi" }}
{{- end }}


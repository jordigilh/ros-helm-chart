{{- if and .Values.postgresql.enabled (not .Values.postgresql.database.existingSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "cost-mgmt-infra.postgresql.secretName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cost-mgmt-infra.labels" . | nindent 4 }}
type: Opaque
data:
  database: {{ .Values.postgresql.database.name | b64enc | quote }}
  username: {{ .Values.postgresql.database.user | b64enc | quote }}
  password: {{ randAlphaNum 32 | b64enc | quote }}
{{- end }}


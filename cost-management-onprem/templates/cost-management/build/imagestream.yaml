apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: {{ include "cost-mgmt.koku.api.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cost-mgmt.koku.labels" . | nindent 4 }}
    app.kubernetes.io/component: koku-image
spec:
  lookupPolicy:
    local: true


apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "cost-mgmt.koku.database.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cost-mgmt.koku.labels" . | nindent 4 }}
    app.kubernetes.io/component: postgresql
    app.kubernetes.io/name: koku-database
spec:
  serviceName: {{ include "cost-mgmt.koku.database.name" . }}
  replicas: 1
  selector:
    matchLabels:
      {{- include "cost-mgmt.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: postgresql
      app.kubernetes.io/name: koku-database
  template:
    metadata:
      labels:
        {{- include "cost-mgmt.koku.labels" . | nindent 8 }}
        app.kubernetes.io/component: postgresql
        app.kubernetes.io/name: koku-database
    spec:
      serviceAccountName: {{ include "cost-mgmt.koku.serviceAccountName" . }}

      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault

      containers:
      - name: postgresql
        image: "{{ .Values.costManagement.database.image.repository }}:{{ .Values.costManagement.database.image.tag }}"
        imagePullPolicy: {{ .Values.costManagement.database.image.pullPolicy }}

        ports:
        - name: postgresql
          containerPort: 5432
          protocol: TCP

        env:
        - name: POSTGRES_DB
          value: {{ .Values.costManagement.database.env.POSTGRES_DB | quote }}
        - name: POSTGRES_USER
          value: {{ .Values.costManagement.database.env.POSTGRES_USER | quote }}
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ include "cost-mgmt.koku.database.secretName" . }}
              key: password
        - name: PGDATA
          value: {{ .Values.costManagement.database.env.PGDATA | quote }}

        livenessProbe:
          exec:
            command:
              - /bin/sh
              - -c
              - pg_isready -U {{ .Values.costManagement.database.env.POSTGRES_USER }}
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 6

        readinessProbe:
          exec:
            command:
              - /bin/sh
              - -c
              - pg_isready -U {{ .Values.costManagement.database.env.POSTGRES_USER }}
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3

        resources:
          {{- toYaml .Values.costManagement.database.resources | nindent 10 }}

        volumeMounts:
        - name: data
          mountPath: /var/lib/postgresql/data

        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
          readOnlyRootFilesystem: false
          runAsNonRoot: true
          seccompProfile:
            type: RuntimeDefault

  volumeClaimTemplates:
  - metadata:
      name: data
      labels:
        {{- include "cost-mgmt.koku.labels" . | nindent 8 }}
    spec:
      accessModes:
        {{- toYaml .Values.costManagement.database.storage.accessModes | nindent 8 }}
      {{- if .Values.costManagement.database.storage.storageClassName }}
      storageClassName: {{ .Values.costManagement.database.storage.storageClassName }}
      {{- end }}
      resources:
        requests:
          storage: {{ .Values.costManagement.database.storage.size }}


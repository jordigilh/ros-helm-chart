apiVersion: v1
kind: Service
metadata:
  name: {{ include "cost-mgmt.fullname" . }}-rosocp-processor
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cost-mgmt.labels" . | nindent 4 }}
    app.kubernetes.io/component: processor
    app.kubernetes.io/name: rosocp-processor
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.ros.processor.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "cost-mgmt.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: processor
    app.kubernetes.io/name: rosocp-processor


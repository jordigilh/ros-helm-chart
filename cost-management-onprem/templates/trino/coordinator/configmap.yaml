apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cost-mgmt.trino.coordinator.name" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cost-mgmt.trino.labels" . | nindent 4 }}
    app.kubernetes.io/component: trino-coordinator
data:
  config.properties: |
    coordinator=true
    node-scheduler.include-coordinator=false
    http-server.http.port=8080
    query.max-memory=1GB
    query.max-memory-per-node=512MB
    discovery.uri=http://localhost:8080

  jvm.config: |
    -server
    -Xmx{{ .Values.trino.coordinator.config.jvm.maxHeapSize }}
    -XX:+Use{{ .Values.trino.coordinator.config.jvm.gcMethod }}
    -XX:G1HeapRegionSize=32M
    -XX:+UseGCOverheadLimit
    -XX:+ExplicitGCInvokesConcurrent
    -XX:+HeapDumpOnOutOfMemoryError
    -XX:+ExitOnOutOfMemoryError
    -Djdk.attach.allowAttachSelf=true

  node.properties: |
    node.environment=production
    node.data-dir=/data/trino

  log.properties: |
    io.trino=INFO

  hive.properties: |
    connector.name=hive
    hive.metastore.uri={{ include "cost-mgmt.trino.metastore.uri" . }}
    hive.non-managed-table-writes-enabled=true
    # Use Hadoop configuration for S3 access (core-site.xml)
    hive.config.resources=/etc/hadoop/conf/core-site.xml

  postgresql.properties: |
    connector.name=postgresql
    connection-url={{ include "cost-mgmt.trino.metastore.database.url" . }}
    connection-user={{ .Values.costManagement.database.user }}
    connection-password=${ENV:POSTGRES_PASSWORD}

  core-site.xml: |
    <?xml version="1.0"?>
    <?xml-stylesheet type="text/xsl" href="configuration.xsl"?>
    <configuration>
      <property>
        <name>fs.s3a.endpoint</name>
        <value>{{ include "cost-mgmt.koku.s3.endpoint" . }}</value>
      </property>
      <property>
        <name>fs.s3a.path.style.access</name>
        <value>true</value>
      </property>
      <property>
        <name>fs.s3a.connection.ssl.enabled</name>
        <value>false</value>
      </property>
      <property>
        <name>fs.s3a.access.key</name>
        <value>${ENV:AWS_ACCESS_KEY_ID}</value>
      </property>
      <property>
        <name>fs.s3a.secret.key</name>
        <value>${ENV:AWS_SECRET_ACCESS_KEY}</value>
      </property>
    </configuration>

